<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="commm.example.android.note">

    <application
        android:theme="@style/AppTheme"
        android:icon="@mipmap/cs"
        android:label="@string/app_name" >
        <activity
            android:label="@string/app_name"
            android:name="commm.example.android.note.MainActivity" >
            <intent-filter >
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity   android:name="commm.example.android.note.EditActivity" />
        <activity  android:name="commm.example.android.note.NewActivity" />

    </application>

</manifest>